// Funkce která simuluje vyhledávání na Googlu a vrací výsledky pro dané klíčové slovo
function fetchGoogleResults(keyword) {
    
    return `Results for ${keyword}`;
  }
  
  // Exportuji funkce pro použití vjiných souborech
  module.exports = { fetchGoogleResults };
  